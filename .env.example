# Server Configuration
PORT=8080
NODE_ENV=development

# Example environment variable
PING_MESSAGE=pong

# Encryption Configuration
# IMPORTANT: Generate a secure 32+ character random string for production
# You can generate one using: openssl rand -base64 32
ENCRYPTION_KEY=your-secret-encryption-key-min-32-chars-long-replace-this-in-production

# WordPress Integration (Optional)
# WP_DEFAULT_TIMEOUT=30000
# WP_MAX_RETRY_ATTEMPTS=3
